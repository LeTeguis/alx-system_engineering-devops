echo '#!/bin/bash' > 0x02-shell_redirections/16-whatsnext

echo 'cat /etc/passwd | grep -A 3 "root"' >> 0x02-shell_redirections/16-whatsnext

chmod u+x 0x02-shell_redirections/16-whatsnext

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
