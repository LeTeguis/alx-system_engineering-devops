echo '#!/bin/bash' > 0x02-shell_redirections/5-firstlines

echo 'head /etc/passwd' >> 0x02-shell_redirections/5-firstlines

chmod u+x 0x02-shell_redirections/5-firstlines

git add .

git commit -m "First lines of a file"

git push

clear

vi custom_script

./custom_script
