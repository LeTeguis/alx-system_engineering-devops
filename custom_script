echo '#!/bin/bash' > 0x03-shell_variables_expansions/3-paths

echo 'echo $PATH | tr -s '\'':'\'' '\\n'\'' | wc -l' >> 0x03-shell_variables_expansions/3-paths

chmod u+x 0x03-shell_variables_expansions/3-paths

git add .

git commit -m "shell_variables_expansions"

git push

clear

vi custom_script

./custom_script
