echo '#!/bin/bash' > 0x02-shell_redirections/11-directories

echo 'find . -mindepth 1 -type d | wc -l' >> 0x02-shell_redirections/11-directories

chmod u+x 0x02-shell_redirections/11-directories

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
