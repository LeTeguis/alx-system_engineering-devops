echo '#!/bin/bash' > 0x02-shell_redirections/8-cwd_state

echo 'ls -la > ls_cwd_content' >> 0x02-shell_redirections/8-cwd_state

chmod u+x 0x02-shell_redirections/8-cwd_state

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
