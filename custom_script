echo '#!/bin/bash' > 0x02-shell_redirections/18-letteronly

echo 'cat /etc/ssh/sshd_config | grep "^[a-zA-Z]"' >> 0x02-shell_redirections/18-letteronly

chmod u+x 0x02-shell_redirections/18-letteronly

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
