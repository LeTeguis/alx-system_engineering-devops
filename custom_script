echo '#!/bin/bash' > 0x03-shell_variables_expansions/2-path

echo 'export PATH=$PATH:/action' >> 0x03-shell_variables_expansions/2-path

chmod u+x 0x03-shell_variables_expansions/2-path

git add .

git commit -m "shell_variables_expansions"

git push

clear

vi custom_script

./custom_script
