echo '#!/bin/bash' > 0x02-shell_redirections/15-countthatword

echo 'cat /etc/passwd | grep "bin" | wc -l' >> 0x02-shell_redirections/15-countthatword

chmod u+x 0x02-shell_redirections/15-countthatword

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
