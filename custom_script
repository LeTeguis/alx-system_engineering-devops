echo '#!/bin/bash' > 0x03-shell_variables_expansions/11-binary_to_decimal

echo 'echo $(( 2#$BINARY ))' >> 0x03-shell_variables_expansions/11-binary_to_decimal

chmod u+x 0x03-shell_variables_expansions/11-binary_to_decimal

git add .

git commit -m "shell_variables_expansions"

git push

clear

vi custom_script

./custom_script
