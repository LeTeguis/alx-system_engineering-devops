echo '#!/bin/bash' > 0x02-shell_redirections/100-empty_casks

echo 'find . -empty -printf '\''%f\n'\' >> 0x02-shell_redirections/100-empty_casks

chmod u+x 0x02-shell_redirections/100-empty_casks

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
