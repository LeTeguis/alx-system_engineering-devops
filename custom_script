echo '#!/bin/bash' > 0x02-shell_redirections/4-lastlines

echo 'tail /etc/passwd' >> 0x02-shell_redirections/4-lastlines

chmod u+x 0x02-shell_redirections/4-lastlines

git add .

git commit -m "Last lines of a file"

git push

clear

vi custom_script

./custom_script
