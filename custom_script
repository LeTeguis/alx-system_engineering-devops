echo '#!/bin/bash' > 0x02-shell_redirections/9-duplicate_last_line

echo 'tail -1 iacta >> iacta' >> 0x02-shell_redirections/9-duplicate_last_line

chmod u+x 0x02-shell_redirections/9-duplicate_last_line

git add .

git commit -m "file"

git push

clear

vi custom_script

./custom_script
