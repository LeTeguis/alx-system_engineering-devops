echo '#!/bin/bash' > 0x03-shell_variables_expansions/12-combinations

echo 'echo {a..z}{a..z} | tr '\' \' \''\n'\'' | grep -v "oo"' >> 0x03-shell_variables_expansions/12-combinations

chmod u+x 0x03-shell_variables_expansions/12-combinations

git add .

git commit -m "shell_variables_expansions"

git push

clear

vi custom_script

./custom_script
